# Model Selection

Modes:
- `disabled`: deterministic heuristics only.
- `local`: local Hugging Face runtime (roadmap).
- `remote`: remote HF inference API.

All modes pass through deterministic validation.
