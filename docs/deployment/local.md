# Local Deployment

1. Copy `.env.example` to `.env`.
2. `docker compose up --build`.
3. Login to UI at `http://localhost:8080`.

Network scanning reminder: only scan networks you own/operate and have permission to inspect.
